## Agent skills

### Issue tracker

Issues and specs live as markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: read the root `CONTEXT.md` and relevant decisions in `docs/adr/`. See `docs/agents/domain.md`.
